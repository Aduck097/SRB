server:
  port: 8130

spring:
  profiles:
    active: dev
  application:
    name: service-oss

aliyun:
  oss:
    endpoint: "endpoint"
    KeyId: "KeyId"
    KeySecret: "KeySecret"
    bucketName: "bucketName"
